name: untitled3
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  speech_to_text: 7.4.0
  confetti: ^0.8.0
  shared_preferences: ^2.3.0
  flutter_tts: ^4.0.2
  audioplayers: ^6.0.0
  # --- Yeni Eklenen Paketler ---
  mobx: ^2.3.3
  flutter_mobx: ^2.2.1+1
  record: ^7.1.1
  path_provider: ^2.1.2
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # --- Yeni Eklenen Kod Üreticiler ---
  build_runner: ^2.4.9
  mobx_codegen: ^2.6.0+1

flutter:
  uses-material-design: true
  assets:
    - assets/resimler/
    - assets/3-4/
    - assets/4-5/
    - assets/5-6/
    - assets/sesler/

